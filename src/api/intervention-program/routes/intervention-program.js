'use strict';

/**
 * intervention-program router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::intervention-program.intervention-program');
